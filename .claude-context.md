```markdown
# Project Summary for AI Coding Agent

**Project Name:** VoxBridge iOS
**Operating System:** Darwin
**Project Root:** `/Users/colinbehr/voxbridge-ios`
**Temporary Directory:** `/Users/colinbehr/.gemini/tmp/voxbridge-ios`
**Git Status:** Managed repository. Recent commits indicate initial build, session timeout enforcement, and completion of real-time interpreter earpiece feature.

## Directory Structure:
- `VoxBridge/`: Main application source.
  - `Info.plist`
  - `VoxBridgeApp.swift`
  - `Models/`: `Language.swift`, `RealtimeMessages.swift`, `SessionState.swift`
  - `Services/`: `AudioCaptureService.swift`, `AudioPlaybackService.swift`, `AudioSessionManager.swift`, `KeychainService.swift`, `RealtimeAPIService.swift`
  - `Utilities/`: `AudioConverter.swift`, `Constants.swift`
  - `ViewModels/`: `InterpreterViewModel.swift`
  - `Views/`: `ContentView.swift`, `ListeningView.swift`, `OnboardingView.swift`, `SettingsView.swift`, `SetupView.swift`, `TranscriptView.swift`, `WaveformView.swift`
- `VoxBridge.xcodeproj/`
- `project.yml`, `README.md`, `.gitignore`

## Recent Development History:
- Initial build with `feat/initial-build`.
- Fixed session timeout enforcement and removed `defaultToSpeaker`.
- Completed real-time interpreter earpiece feature.
- Initial commit with `.gitignore` and `README`.

## Constraints/Special Instructions:
- **Pencil MCP Server:** This server is active and dedicated to `.pen` files.
  - **Crucial:** `.pen` files are encrypted. **NEVER** use `Read` or `Grep` tools on `.pen` files.
  - **Mandatory:** ONLY use `pencil` MCP tools (`batch_get`, `batch_design`, `get_editor_state`, `open_document`, `get_guidelines`, `get_style_guide_tags`, `get_style_guide`, `snapshot_layout`, `get_screenshot`, `get_variables`, `set_variables`, `find_empty_space_on_canvas`, `search_all_unique_properties`, `replace_all_matching_properties`, `export_nodes`) for all interactions with `.pen` files.
  - **`batch_design` operations:** Aim for max 25 operations per call. Follow specified syntax for `Insert`, `Copy`, `Replace`, `Update`, `Delete`, `Move`, `Generate image`.
```
